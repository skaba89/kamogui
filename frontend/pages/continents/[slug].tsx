import { useRouter } from 'next/router'; import Layout from '../../components/Layout'; import { continents } from '../../data/locations'; export default function ContinentPage(){const {query}=useRouter(); const data=continents.find(c=>c.slug===query.slug); if(!data)return <Layout><div className="container"><h1>Chargement...</h1></div></Layout>; return <Layout><section className="pageHero"><div className="container"><p className="eyebrow">Présence internationale</p><h1>{data.name}</h1><p className="lead">{data.address}</p></div></section><section className="content"><div className="container"><div className="card"><h3>Coordonnées</h3><p className="muted">Adresse : {data.address}<br/>Téléphone : {data.phone}<br/>Email : {data.email}</p><a className="btn btnGold" href={`mailto:${data.email}`}>Contacter ce bureau</a><iframe className="mapFrame" src={data.map} loading="lazy"></iframe></div></div></section></Layout>}
