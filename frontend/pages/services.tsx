import Link from 'next/link'; import Layout from '../components/Layout'; import { services } from '../data/services'; export default function Services(){return <Layout><section className="pageHero"><div className="container"><p className="eyebrow">Services</p><h1>Services corporate</h1><p className="lead">Une offre complète pour sécuriser, documenter et valoriser les opérations aurifères.</p></div></section><section className="content"><div className="container grid">{services.map(s=><Link className="card" href={`/services/${s.slug}`} key={s.slug} style={{textDecoration:'none'}}><h3>{s.title}</h3><p className="muted">{s.desc}</p><span style={{color:'#D4AF37'}}>Voir détail →</span></Link>)}</div></section></Layout>}
