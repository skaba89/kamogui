import Layout from '../components/Layout'; import { continents } from '../data/locations'; import Link from 'next/link'; export default function Contact(){return <Layout><section className="pageHero"><div className="container"><p className="eyebrow">Contact</p><h1>Présence internationale</h1><p className="lead">Contactez le bureau régional concerné.</p></div></section><section className="content"><div className="container grid">{continents.map(c=><Link className="card" href={`/continents/${c.slug}`} key={c.slug} style={{textDecoration:'none'}}><h3>{c.name}</h3><p className="muted">{c.address}<br/>{c.phone}<br/>{c.email}</p><span style={{color:'#D4AF37'}}>Voir la carte →</span></Link>)}</div></section></Layout>}
