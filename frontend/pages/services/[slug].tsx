import { useRouter } from 'next/router'; import Layout from '../../components/Layout'; import { services } from '../../data/services'; import Link from 'next/link'; export default function ServicePage(){const {query}=useRouter(); const s=services.find(x=>x.slug===query.slug); if(!s)return <Layout><div className="container"><h1>Chargement...</h1></div></Layout>; return <Layout><section className="pageHero"><div className="container"><p className="eyebrow">Service</p><h1>{s.title}</h1><p className="lead">{s.desc}</p></div></section><section className="content"><div className="container" style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(320px,1fr))',gap:24}}><div className="card"><h3>Ce service inclut</h3>{s.bullets.map(b=><p className="muted" key={b}>✓ {b}</p>)}</div><div className="card"><h3>Objectif business</h3><p className="muted">Transformer une opportunité en dossier crédible, contrôlable et exploitable par des partenaires institutionnels.</p><Link className="btn btnGold" href="/contact">Demander une consultation</Link></div></div></section></Layout>}
