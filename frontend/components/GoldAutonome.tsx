import { useState } from 'react'
type Msg = { role: 'assistant' | 'user'; content: string }
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'https://kamogui.onrender.com'
export default function GoldAutonome(){
 const [chatOpen,setChatOpen]=useState(false); const [messages,setMessages]=useState<Msg[]>([{role:'assistant',content:'Bonjour, je suis Gold Autonome. Je peux vous orienter sur la conformité, la traçabilité, l’investissement et les partenariats KAMOGUI.'}]); const [input,setInput]=useState(''); const [loading,setLoading]=useState(false)
 async function sendMessage(){ if(!input.trim()||loading)return; const text=input.trim(); setInput(''); setMessages(m=>[...m,{role:'user',content:text}]); setLoading(true); try{const r=await fetch(`${API_URL}/api/chat`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text})}); const d=await r.json(); setMessages(m=>[...m,{role:'assistant',content:d.reply||'Réponse indisponible.'}])}catch{setMessages(m=>[...m,{role:'assistant',content:'Connexion backend indisponible. Vérifiez Render et NEXT_PUBLIC_API_URL.'}])}finally{setLoading(false)} }
 return <><button className="btn btnGold chatButton" onClick={()=>setChatOpen(!chatOpen)}>{chatOpen?'Fermer':'Gold Autonome'}</button>{chatOpen&&<div className="chatBox"><h3 style={{color:'#D4AF37',marginTop:0}}>Gold Autonome</h3><div className="messages">{messages.map((m,i)=><div key={i} className={`msg ${m.role}`}>{m.content}</div>)}{loading&&<small style={{color:'#D4AF37'}}>Analyse en cours...</small>}</div><div style={{display:'flex',gap:8,marginTop:12}}><input className="input" value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&sendMessage()} placeholder="Question confidentielle..."/><button className="btn btnGold" onClick={sendMessage}>OK</button></div></div>}</>
}
